apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: personal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: ecommerce
            - name: POSTGRES_PASSWORD
              value: ecommerce
            - name: POSTGRES_DB
              value: ecommerce
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "ecommerce"]
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: personal
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
